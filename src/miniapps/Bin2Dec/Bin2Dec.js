import React from 'react';

const Bin2Dec = () => {
  // .match(/^[0-1]+$/g)

  // var checkIfOnlyCharactersInString = (charactersList, stringInQuestion) => stringInQuestion.split('').every(stringElement => charactersList.includes(stringElement));

  // var inputString = '00010011';
  // if(!checkIfOnlyCharactersInString(['0','1'], inputString))
  //     window.alert('A character other than a 0 or 1 digit was entered. Please correct the input.');
  // else
  //     console.log(parseInt(inputString, 2))

  return <div>Bin2Dec contents</div>;
};

export default Bin2Dec;
